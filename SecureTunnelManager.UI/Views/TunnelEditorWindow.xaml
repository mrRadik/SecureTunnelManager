<windows:StmChromeWindow x:Class="SecureTunnelManager.UI.Views.TunnelEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:SecureTunnelManager.UI.Views.Controls"
        xmlns:windows="clr-namespace:SecureTunnelManager.UI.Windows"
        xmlns:vm="clr-namespace:SecureTunnelManager.UI.ViewModels"
        xmlns:loc="clr-namespace:SecureTunnelManager.UI.Localization"
        Title="{Binding WindowTitle}"
        Style="{DynamicResource StmWindow}"
        MinWidth="900"
        MinHeight="680"
        Width="980"
        Height="760"
        WindowStartupLocation="CenterOwner">

    <Grid Background="{DynamicResource StmBackgroundBrush}"
          SnapsToDevicePixels="True"
          UseLayoutRounding="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <controls:WindowTitleBar Grid.Row="0" Title="{Binding WindowTitle}"/>

            <StackPanel Grid.Row="1" Margin="24,12,24,0">
                <TextBlock Text="{Binding StepIndicator}" Style="{DynamicResource StmPageSubtitle}"/>
                <TextBlock Text="{Binding CurrentStepTitle}" Style="{DynamicResource StmCardTitle}" Margin="0,4,0,0"/>
            </StackPanel>

            <Grid Grid.Row="2" Margin="24,12,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Wizard steps -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- Step 1 -->
                    <Border Style="{DynamicResource StmCard}" Margin="0,0,0,16"
                            Visibility="{Binding CurrentStep, Converter={StaticResource IntEqualsConverter}, ConverterParameter=0}">
                        <StackPanel>
                            <TextBlock Text="Give this tunnel a recognizable name and optional notes."
                                       Style="{DynamicResource StmPageSubtitle}" Margin="0,0,0,16"/>
                            <TextBlock Text="Name" Style="{DynamicResource StmLabel}"/>
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource StmInput}"/>
                            <TextBlock Text="{Binding NameError}" Style="{DynamicResource StmValidationError}"
                                       Visibility="{Binding NameError, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                            <TextBlock Text="Description" Style="{DynamicResource StmLabel}"/>
                            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource StmInput}"
                                     MinHeight="72" AcceptsReturn="True" TextWrapping="Wrap" VerticalContentAlignment="Top"/>
                            <TextBlock Text="{loc:Loc Editor.Icon}" Style="{DynamicResource StmLabel}" Margin="0,12,0,0"/>
                            <TextBlock Text="{loc:Loc Editor.IconHint}"
                                       Style="{DynamicResource StmPageSubtitle}"
                                       Margin="0,0,0,8"/>
                            <controls:ServiceIconPicker SelectedIconKey="{Binding IconKey, Mode=TwoWay}"
                                                        FallbackKey="tunnel"/>
                            <CheckBox Content="Connect automatically when Windows starts"
                                      IsChecked="{Binding StartWithWindows}" Style="{DynamicResource StmCheckBox}" Margin="0,16,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Step 2 -->
                    <Border Style="{DynamicResource StmCard}" Margin="0,0,0,16"
                            Visibility="{Binding CurrentStep, Converter={StaticResource IntEqualsConverter}, ConverterParameter=1}">
                        <StackPanel>
                            <TextBlock Text="Intermediate servers between your PC and the target. Add more if your route uses multiple jump hosts."
                                       Style="{DynamicResource StmPageSubtitle}" Margin="0,0,0,16"/>

                            <ItemsControl ItemsSource="{Binding JumpHosts}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate DataType="{x:Type vm:JumpHostHopViewModel}">
                                        <controls:JumpHostHopEditor/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Button Content="+ Add jump host"
                                    Command="{Binding AddJumpHostCommand}"
                                    Style="{DynamicResource StmSecondaryButton}"
                                    HorizontalAlignment="Left"/>
                        </StackPanel>
                    </Border>

                    <!-- Step 3 -->
                    <Border Style="{DynamicResource StmCard}" Margin="0,0,0,16"
                            Visibility="{Binding CurrentStep, Converter={StaticResource IntEqualsConverter}, ConverterParameter=2}">
                        <StackPanel>
                            <TextBlock Text="The server and service you want to reach through the jump host."
                                       Style="{DynamicResource StmPageSubtitle}" Margin="0,0,0,16"/>
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                    <TextBlock Text="Host address" Style="{DynamicResource StmLabel}"/>
                                    <TextBox Text="{Binding TargetHost, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource StmInput}"/>
                                    <TextBlock Text="{Binding TargetHostError}" Style="{DynamicResource StmValidationError}"
                                               Visibility="{Binding TargetHostError, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Port" Style="{DynamicResource StmLabel}"/>
                                    <TextBox Text="{Binding TargetPort, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource StmInput}"/>
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="Sign-in name" Style="{DynamicResource StmLabel}"/>
                            <TextBox Text="{Binding TargetUsername, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource StmInput}"/>
                            <TextBlock Text="{Binding TargetUsernameError}" Style="{DynamicResource StmValidationError}"
                                       Visibility="{Binding TargetUsernameError, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>

                            <TextBlock Text="Sign-in method" Style="{DynamicResource StmLabel}" Margin="0,16,0,8"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <RadioButton Content="Password"
                                             GroupName="TargetAuthMethod"
                                             Style="{DynamicResource StmRadioButton}"
                                             IsChecked="{Binding IsTargetPasswordAuth, Mode=TwoWay}"/>
                                <RadioButton Content="Certificate (key file)"
                                             GroupName="TargetAuthMethod"
                                             Style="{DynamicResource StmRadioButton}"
                                             IsChecked="{Binding IsTargetPrivateKeyAuth, Mode=TwoWay}"/>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsTargetPasswordAuth, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="Password" Style="{DynamicResource StmLabel}"/>
                                <PasswordBox x:Name="TargetPasswordBox"
                                             Style="{DynamicResource StmInputPassword}"
                                             PasswordChanged="OnTargetPasswordChanged"/>
                                <TextBlock Text="Leave blank when editing to keep the saved password."
                                           Style="{DynamicResource StmPageSubtitle}"
                                           Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                <TextBlock Text="{Binding TargetCredentialError}" Style="{DynamicResource StmValidationError}"
                                           Visibility="{Binding TargetCredentialError, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                            </StackPanel>

                            <StackPanel Visibility="{Binding IsTargetPrivateKeyAuth, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="Key file" Style="{DynamicResource StmLabel}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding TargetPrivateKeyPath, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource StmInput}"/>
                                    <Button Grid.Column="1" Content="Browse" Command="{Binding BrowseTargetKeyCommand}" Style="{DynamicResource StmSecondaryButton}" Margin="8,0,0,4" MinWidth="90"/>
                                </Grid>
                                <TextBlock Text="{Binding TargetPrivateKeyError}" Style="{DynamicResource StmValidationError}"
                                           Visibility="{Binding TargetPrivateKeyError, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                                <TextBlock Text="Key passphrase (optional)" Style="{DynamicResource StmLabel}"/>
                                <PasswordBox x:Name="TargetKeyPassphraseBox" Style="{DynamicResource StmInputPassword}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Step 4 -->
                    <Border Style="{DynamicResource StmCard}" Margin="0,0,0,16"
                            Visibility="{Binding CurrentStep, Converter={StaticResource IntEqualsConverter}, ConverterParameter=3}">
                        <StackPanel>
                            <TextBlock Text="Choose a local address and port on this PC, then the service port on the target server."
                                       Style="{DynamicResource StmPageSubtitle}" Margin="0,0,0,16"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                    <TextBlock Text="Local address" Style="{DynamicResource StmLabel}"/>
                                    <TextBox Text="{Binding LocalBindAddress, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource StmInput}"/>
                                    <TextBlock Text="{Binding LocalBindAddressError}" Style="{DynamicResource StmValidationError}"
                                               Visibility="{Binding LocalBindAddressError, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Local port" Style="{DynamicResource StmLabel}"/>
                                    <TextBox Text="{Binding LocalPort, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource StmInput}"/>
                                    <TextBlock Text="{Binding LocalPortError}" Style="{DynamicResource StmValidationError}"
                                               Visibility="{Binding LocalPortError, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                                </StackPanel>
                            </Grid>
                            <TextBlock Text="Service port on target" Style="{DynamicResource StmLabel}" Margin="0,12,0,6"/>
                            <TextBox Text="{Binding RemotePort, UpdateSourceTrigger=PropertyChanged}" Style="{DynamicResource StmInput}" MaxWidth="200" HorizontalAlignment="Left"/>

                            <Border Margin="0,16,0,0"
                                    Padding="12"
                                    CornerRadius="6"
                                    Background="{DynamicResource StmSurfaceBrush}"
                                    BorderBrush="{DynamicResource StmBorderBrush}"
                                    BorderThickness="1"
                                    Visibility="{Binding HasTestResult, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="{Binding TestMessage}" TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource StmBody}">
                                            <Setter Property="Foreground" Value="{DynamicResource StmTextPrimaryBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding TestSucceeded}" Value="True">
                                                    <Setter Property="Foreground" Value="{DynamicResource StmSuccessBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding TestSucceeded}" Value="False">
                                                    <Setter Property="Foreground" Value="{DynamicResource StmErrorBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <!-- Connection diagram -->
            <Border Grid.Column="2" Style="{DynamicResource StmCard}" Margin="0" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="Connection path" Style="{DynamicResource StmCardTitle}"/>
                    <TextBlock Text="How traffic flows from your PC to the remote service."
                               Style="{DynamicResource StmPageSubtitle}" Margin="0,-8,0,16"/>

                    <Border Background="{DynamicResource StmSurfaceBrush}" CornerRadius="8" Padding="16,12" Margin="0,0,0,8">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Local PC" Style="{DynamicResource StmLabel}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding FlowMyComputer}" Style="{DynamicResource StmBody}" FontWeight="SemiBold" HorizontalAlignment="Center" FontFamily="Consolas"/>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="│" Style="{DynamicResource StmBody}" HorizontalAlignment="Center" Opacity="0.5"/>
                    <ItemsControl ItemsSource="{Binding FlowJumpHosts}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <Border Background="{DynamicResource StmSurfaceBrush}" CornerRadius="8" Padding="16,12" Margin="0,8">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="Jump Host" Style="{DynamicResource StmLabel}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                            <TextBlock Text="{Binding}" Style="{DynamicResource StmBody}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <TextBlock Text="│" Style="{DynamicResource StmBody}" HorizontalAlignment="Center" Opacity="0.5"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Border Background="{DynamicResource StmSurfaceBrush}" CornerRadius="8" Padding="16,12" Margin="0,8">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Target Server" Style="{DynamicResource StmLabel}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding FlowTargetServer}" Style="{DynamicResource StmBody}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="│" Style="{DynamicResource StmBody}" HorizontalAlignment="Center" Opacity="0.5"/>
                    <Border Background="{DynamicResource StmSelectedBrush}" CornerRadius="8" Padding="16,12" Margin="0,8,0,0">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Forwarded service" Style="{DynamicResource StmLabel}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="{Binding FlowForwardedService}" Style="{DynamicResource StmBody}" FontWeight="SemiBold" HorizontalAlignment="Center" FontFamily="Consolas"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="3" Style="{DynamicResource StmDialogFooter}">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Content="Back" Command="{Binding BackCommand}" Style="{DynamicResource StmGhostButton}"
                            Visibility="{Binding IsFirstStep, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                    <Button Click="OnTestClick"
                            Visibility="{Binding IsLastStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource StmSecondaryButton}">
                                <Setter Property="Margin" Value="8,0,0,0"/>
                                <Setter Property="Content" Value="{Binding TestTunnelButtonText}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsTesting}" Value="True">
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
                <TextBlock Text="{Binding ErrorMessage}" Style="{DynamicResource StmValidationError}" VerticalAlignment="Center" HorizontalAlignment="Center"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" IsCancel="True" Style="{DynamicResource StmGhostButton}" Margin="0,0,8,0"/>
                    <Button Content="Next" Click="OnNextClick" Style="{DynamicResource StmPrimaryButton}" Margin="0,0,8,0"
                            Visibility="{Binding IsLastStep, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                    <Button Content="Save Tunnel" Click="OnSaveClick" Style="{DynamicResource StmSuccessButton}"
                            Visibility="{Binding IsLastStep, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</windows:StmChromeWindow>
