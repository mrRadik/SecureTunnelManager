<UserControl x:Class="SecureTunnelManager.UI.Views.Controls.MetricCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="150">

    <Border Style="{StaticResource StmMetricCard}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Ellipse Grid.Column="0" Width="10" Height="10" VerticalAlignment="Top" Margin="0,6,12,0"
                     Fill="{Binding DotBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"/>

            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           Style="{StaticResource StmLabel}" Margin="0,0,0,4"/>
                <TextBlock Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
                           FontFamily="Segoe UI"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Foreground="{Binding ValueBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
