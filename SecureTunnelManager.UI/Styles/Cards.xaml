<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="StmCardIconHost" TargetType="Border">
        <Setter Property="Width" Value="32"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="CornerRadius" Value="{StaticResource StmControlCornerRadius}"/>
        <Setter Property="Background" Value="{DynamicResource StmSurfaceBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="StmCardIconGlyph" TargetType="TextBlock" BasedOn="{StaticResource StmIconGlyph}">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Foreground" Value="{DynamicResource StmAccentBrush}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>

    <Style x:Key="StmCardActionButton" TargetType="Button" BasedOn="{StaticResource StmActionButton}">
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Margin" Value="0"/>
    </Style>

    <Style x:Key="StmCardMenuButton" TargetType="Button" BasedOn="{StaticResource StmIconButton}">
        <Setter Property="MinWidth" Value="30"/>
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <Style x:Key="StmRouteHopPill" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource StmSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StmBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="StmRouteHopText" TargetType="TextBlock" BasedOn="{StaticResource StmFontBase}">
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource StmTextPrimaryBrush}"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style x:Key="StmRouteChainArrow" TargetType="TextBlock">
        <Setter Property="Text" Value="&#x2192;"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource StmRouteConnectorBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="6,0"/>
        <Setter Property="Opacity" Value="0.85"/>
    </Style>

    <Style x:Key="StmInteractiveCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource StmCardBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StmBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource StmCardCornerRadius}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource StmHoverBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource StmAccentBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource StmAccentBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="StmRdpComputerCard" TargetType="Border" BasedOn="{StaticResource StmInteractiveCard}">
        <Setter Property="MinHeight" Value="52"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
    </Style>

    <Style x:Key="StmRdpGridComputerCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource StmCardBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StmBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource StmCardCornerRadius}"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource StmCardHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource StmAccentBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="StmRdpGridCardTitle" TargetType="TextBlock" BasedOn="{StaticResource StmFontBase}">
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource StmTextPrimaryBrush}"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style x:Key="StmRdpGridCardDescription" TargetType="TextBlock" BasedOn="{StaticResource StmFontBase}">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{DynamicResource StmTextSecondaryBrush}"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style x:Key="StmRdpGridCardCaption" TargetType="TextBlock" BasedOn="{StaticResource StmFontBase}">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource StmTextSecondaryBrush}"/>
        <Setter Property="Opacity" Value="0.72"/>
        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
    </Style>

    <Style x:Key="StmRdpGridRoutePill" TargetType="Border" BasedOn="{StaticResource StmRouteHopPill}">
        <Setter Property="Padding" Value="7,3"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>

    <Style x:Key="StmRdpGridRouteText" TargetType="TextBlock" BasedOn="{StaticResource StmRouteHopText}">
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="Foreground" Value="{DynamicResource StmTextSecondaryBrush}"/>
        <Setter Property="Opacity" Value="0.9"/>
    </Style>

    <Style x:Key="StmRdpCardActionPanel" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource StmSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StmBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Margin" Value="0,14,0,0"/>
    </Style>

    <Style x:Key="StmRdpGridConnectButton" TargetType="Button" BasedOn="{StaticResource StmPrimaryButton}">
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="Padding" Value="12,0"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Stretch"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource StmAccentHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StmRdpGridDisconnectButton" TargetType="Button" BasedOn="{StaticResource StmDestructiveButton}">
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="Padding" Value="12,0"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource StmDestructiveHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StmRdpCardMenuButtonCompact" TargetType="Button" BasedOn="{StaticResource StmCardMenuButton}">
        <Setter Property="MinWidth" Value="28"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="Margin" Value="4,0,0,0"/>
        <Setter Property="Opacity" Value="0.85"/>
    </Style>

    <Style x:Key="StmTunnelCard" TargetType="Border" BasedOn="{StaticResource StmInteractiveCard}">
        <Setter Property="Margin" Value="0,0,0,6"/>
        <Setter Property="MinHeight" Value="52"/>
    </Style>

    <Style x:Key="StmCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource StmCardBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StmBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource StmCardCornerRadius}"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Margin" Value="16"/>
    </Style>

    <Style x:Key="StmMetricCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource StmCardBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StmBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="{StaticResource StmCardCornerRadius}"/>
        <Setter Property="Padding" Value="16"/>
        <Setter Property="Margin" Value="0,0,12,0"/>
        <Setter Property="MinWidth" Value="150"/>
    </Style>

    <Style x:Key="StmStatCard" TargetType="Border" BasedOn="{StaticResource StmMetricCard}"/>

    <Style x:Key="StmListRow" TargetType="Border">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="0"/>
    </Style>

    <Style x:Key="StmListRowHover" TargetType="Border" BasedOn="{StaticResource StmListRow}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource StmHoverBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="StmWizardStep" TargetType="Border">
        <Setter Property="CornerRadius" Value="16"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Background" Value="{DynamicResource StmSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StmBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style x:Key="StmWizardStepActive" TargetType="Border" BasedOn="{StaticResource StmWizardStep}">
        <Setter Property="Background" Value="{DynamicResource StmSelectedBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource StmAccentBrush}"/>
    </Style>

</ResourceDictionary>
